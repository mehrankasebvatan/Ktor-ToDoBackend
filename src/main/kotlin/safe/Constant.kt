package com.example.safe

import org.flywaydb.core.internal.sqlscript.ParserSqlScript

object Constant {
    const val DATABASE_URL = "jdbc:mariadb://localhost:3306/todo_db"
    const val USERNAME = "ktor_user"
    const val PASSWORD = "ktor_password"
}